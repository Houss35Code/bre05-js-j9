// vos imports ici
// Import des variables depuis variables.js
import { question, answer } from './variables.js';

window.addEventListener("DOMContentLoaded", () => {
   // Affichage dans la console
   console.log(question);
   console.log(answer);
});

